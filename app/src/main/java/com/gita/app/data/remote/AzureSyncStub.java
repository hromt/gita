package com.gita.app.data.remote;

/**
 * Placeholder for Azure App Service integration: expose REST endpoints that mirror Room entities,
 * push local changes on a schedule, and pull remote updates. This demo uses SQLite/Room only;
 * replace this stub with Retrofit/OkHttp clients pointed at your App Service URL when ready.
 */
public final class AzureSyncStub {

    private AzureSyncStub() {
    }

    // Future: syncUsers(), syncItems(), syncTrades()
}
